/**
 * Error caused by invalid user input (uploaded file or request parameters).
 * The message is user-facing (Hebrew) and is returned with status 400.
 */
export class UserInputError extends Error {}
